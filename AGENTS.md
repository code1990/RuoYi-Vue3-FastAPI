# RuoYi 前端模型展示规范

## 发布流程

后端修改和 SQL 迁移在服务器执行并重启 `ruoyi-fastapi.service`；前端只提交源码，不在服务器或此环境安装依赖、构建。Windows 拉取前端代码后负责编译，再将产物发布到服务器。

新增量化模型、选股策略或股票监控页面时，必须提供真实 API 驱动的可视化页面，不得只提交后端计算或静态假数据。

股票列表页面统一使用以下固定前置列，顺序不可改变：

1. `交易日`
2. `股票`
3. `行业`
4. `概念`

这四列必须使用 `fixed="left"`，后续列再展示排名、评分、模型因子、分类等内容。收益列统一按 `T+1`、`T+2`、`T+3`、`T+4`、`T+5` 顺序排列，复用 `src/views/stock` 现有页面的列宽、分页、横向滚动和收益颜色样式。缺失收益显示 `-` 或待完成状态，不能填充推测值。
